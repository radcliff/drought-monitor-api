source 'https://rubygems.org'

gem 'rails', '4.1.1'
gem 'rails-api' #Rails::API provides lightweight, slightly faster version of Rails for API-only apps

# Spring speeds up development by keeping your application running in the background
gem 'spring', group: :development

# Use postgresql as the database for Active Record
gem 'pg'
gem 'activerecord-postgis-adapter' # provides a special ActiveRecord adapter for talking to PostGIS and handling geospatial data

# rails_12factor provides static asset serving and logging on Heroku
gem 'rails_12factor', group: :production

# Use RGeo, a geospatial data library for Ruby, and related modules
gem 'rgeo'
gem 'rgeo-geojson'
gem 'rgeo-shapefile'
gem 'dbf'

gem 'execjs'

gem 'typhoeus'
gem 'rubyzip'

gem 'pry'
gem 'pry-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.1.2'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano', :group => :development

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

# Rails::API requires ruby 1.9.3
ruby '1.9.3'
